<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo4.mapper.CreditCardMapper">

    <insert id="insertCard" parameterType="com.example.demo4.entity.CreditCard">
        INSERT INTO "credit_card" (card_number, card_holder, expiry, cvv)
        VALUES (#{cardNumber}, #{cardHolder}, #{expiry}, #{cvv})
    </insert>

    <select id="selectCardById" parameterType="long" resultType="com.example.demo4.entity.CreditCard">
        SELECT id, card_number AS cardNumber, card_holder AS cardHolder, expiry, cvv
        FROM "credit_card"
        WHERE id = #{id}
    </select>

    <select id="findallCreditCards" resultType="com.example.demo4.entity.CreditCard">
        SELECT id, card_number AS cardNumber, card_holder AS cardHolder, expiry, cvv
        FROM "credit_card"
    </select>

    <delete id="deleteCardById" parameterType="long">
        DELETE FROM "credit_card" WHERE id = #{id}
    </delete>

</mapper>
