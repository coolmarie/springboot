<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.demo4.mapper.UserMapper">
    <select id="findAll" resultType="com.example.demo4.entity.User">
    SELECT id, name, email FROM "user"
    </select>
    <insert id="insertUser" parameterType="com.example.demo4.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO "user" (name, email) VALUES (#{name}, #{email})
    </insert>

    <update id="updateUser" parameterType="com.example.demo4.entity.User">
        UPDATE "user" SET name = #{name}, email = #{email} WHERE id = #{id}
    </update>

    <delete id="deleteUser" parameterType="long">
        DELETE FROM "user" WHERE id = #{id}
    </delete>

</mapper>
